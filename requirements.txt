# IDBI Unified Agentic Platform — ML Pipeline
# Compatible with Python 3.11+ (EC2 Amazon Linux 2023 / Ubuntu 22.04)

# --- Data wrangling ---
pandas>=2.0.0
numpy>=1.24.0

# --- ML ---
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# --- Explainability ---
shap>=0.43.0
matplotlib>=3.7.0

# --- Model persistence ---
joblib>=1.3.0

# --- Scoring API ---
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
gunicorn>=21.0.0

# --- Agent Framework ---
strands-agents>=0.1.0
strands-agents-tools>=0.1.0
httpx>=0.27.0

# --- Notes ---
# gunicorn added for production deployment (systemd + gunicorn + uvicorn workers)
